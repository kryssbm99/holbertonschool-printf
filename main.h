#ifndef MAIN_H_
#define MAIN_H_
#include <stdio.h>
#include <stdlib.h>
#include <limits.h>

int _printf(const char *format, ...)
int _putchar(char c);
int _puts(char *str);
int _printf(const char *format, ...);

#endif
